package Users;

public class HManager extends User{

    /**
     * @param username
     * @param password
     * @param fullName
     * @param role
     */
    public HManager(String username, String password, String fullName, String role) {
        super(username, password, fullName, role);
    }
}
