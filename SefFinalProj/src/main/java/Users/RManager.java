package Users;

public class RManager extends User{
    /**
     * @param username
     * @param password
     * @param fullName
     * @param role
     */
    public RManager(String username, String password, String fullName, String role) {
        super(username, password, fullName, role);
    }
}
